import FieldsPanel from "./ui/panel";
import styles from "./style.module.scss";

const Fields = () => {
  return (
    <section className={styles.section}>
      <FieldsPanel />
    </section>
  );
};

export default Fields;
