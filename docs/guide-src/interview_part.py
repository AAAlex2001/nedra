"""Часть VIII. Топ-50 вопросов с собеседований: глава 49."""


def mono(text):
    return f"<font name='Mono' size='9'>{text}</font>"


def build(P, B, C, note, table, Spacer, PageBreak, mm, CODE_BG, GOOD_BG):
    S = []
    A = S.append
    m = mono

    def q(number, question, *answers, ref=None):
        title = f"{number}. {question}"
        if ref:
            title += f" <font color='#8a8f9c' size='9'>→ глава {ref}</font>"
        A(P(title, "h2"))
        for text in answers:
            A(P(text))

    A(PageBreak())
    A(P("Часть VIII. Вопросы с собеседований", "part"))
    A(P("49. Топ-50 вопросов и коротких ответов", "h1"))
    A(P("Формат: вопрос, ответ на две-пять фраз, который можно произнести вслух, и ссылка на "
        "главу, где тема разобрана подробно. Отвечать заученно не нужно: интервьюер обычно "
        "ждёт, что вы назовёте суть и приведёте пример из своего проекта. Примеры здесь — из "
        "nedra, ими и пользуйтесь."))

    # ---------- сервер и асинхронность ----------
    A(P("Веб-сервер и асинхронность", "h1"))

    q(1, "Что такое WSGI и ASGI и чем они отличаются?",
      "Это два стандарта интерфейса между веб-сервером и Python-приложением. WSGI — "
      "синхронный: сервер вызывает функцию приложения, та возвращает ответ, один поток "
      "обслуживает один запрос. На нём работают Flask и классический Django. ASGI — "
      "асинхронный наследник: приложение — это " + m("async") + "-функция, которая может "
      "отдавать управление на время ожидания, поэтому один процесс держит много "
      "одновременных запросов. Плюс ASGI умеет WebSocket и долгоживущие соединения, WSGI — "
      "нет. FastAPI — ASGI-приложение.", ref=37)

    q(2, "Зачем нужен uvicorn, если есть FastAPI?",
      "FastAPI — это приложение: маршруты, валидация, зависимости. Оно само не умеет "
      "слушать порт и разбирать HTTP. Это делает ASGI-сервер uvicorn: принимает TCP-соединения, "
      "парсит запросы, вызывает приложение и отправляет ответы. В нашем контейнере команда "
      + m("uvicorn app.main:app --host 0.0.0.0 --port 8000") + " как раз запускает сервер и "
      "передаёт ему объект приложения. Перед ним стоит nginx, который терминирует TLS и "
      "раздаёт статику.")

    q(3, "Что такое event loop и что делает await?",
      "Event loop — цикл, который по очереди выполняет задачи в одном потоке. Когда задача "
      "доходит до " + m("await") + " на операции ввода-вывода (запрос к базе, HTTP, сон), она "
      "говорит циклу «я жду», и цикл переключается на другую задачу. Когда данные пришли, "
      "первая задача продолжается с того же места. Так один поток обслуживает сотни запросов, "
      "которые в основном ждут базу. " + m("await") + " — это точка, где можно переключиться; "
      "код между двумя " + m("await") + " выполняется без прерываний.", ref=37)

    q(4, "Что такое GIL и почему асинхронность помогает, несмотря на него?",
      "GIL — глобальная блокировка интерпретатора: в один момент времени байткод Python "
      "выполняет только один поток, поэтому потоки не ускоряют вычисления. Но веб-бэкенд "
      "почти не вычисляет, он ждёт: базу, сеть, диск. Во время ожидания GIL не нужен, и "
      "асинхронность позволяет одному потоку заполнить эти паузы другой работой. Для "
      "процессорных задач нужны процессы: несколько воркеров uvicorn или отдельный сервис.")

    q(5, "Чем в FastAPI отличается ручка def от async def?",
      m("async def") + " выполняется прямо в event loop, и внутри неё нельзя блокировать: "
      "только " + m("await") + "-совместимые библиотеки, у нас asyncpg и aiosmtplib. Обычную "
      + m("def") + " FastAPI запускает в пуле потоков, чтобы она не останавливала цикл; там "
      "можно звать блокирующий код, но пул ограничен, по умолчанию сорок потоков. Смешивать "
      "опасно: блокирующий вызов внутри " + m("async def") + " останавливает всё приложение "
      "на время вызова.", ref=37)

    q(6, "Что случится, если внутри async def вызвать time.sleep(5) или psycopg2?",
      "Event loop заблокируется на пять секунд: ни один другой запрос не будет обработан, "
      "health check не ответит, сайт «зависнет». Поэтому в асинхронном коде используют "
      + m("asyncio.sleep") + ", асинхронные драйверы и клиенты, а тяжёлую синхронную работу "
      "выносят в " + m("run_in_threadpool") + " или в фоновую задачу. Наш пример: запись "
      "загруженного файла на диск в " + m("write_limited") + " синхронная, но порциями по "
      "мегабайту, для картинок это приемлемо; для больших файлов брали бы aiofiles.")

    q(7, "Сколько воркеров uvicorn запускать?",
      "Один воркер — один процесс с одним event loop; он держит много соединений, но "
      "использует одно ядро. Практическое правило: число воркеров около числа ядер, через "
      + m("--workers N") + " или gunicorn с uvicorn-воркерами. У каждого воркера свой пул "
      "соединений к базе, поэтому суммарно " + m("workers × pool_size") + " не должно "
      "превышать лимит PostgreSQL, по умолчанию сто соединений. Для нашего сайта хватает "
      "одного воркера.", ref=37)

    q(8, "Зачем BackgroundTasks и чем они отличаются от Celery?",
      m("BackgroundTasks") + " выполняет функцию после отправки ответа, в том же процессе. "
      "Подходит для быстрых побочных действий: у нас так уходит письмо о заявке, клиент не "
      "ждёт SMTP. Минусы: если процесс упадёт, задача потеряется, повторов и очереди нет. "
      "Celery и подобные — отдельные воркеры с брокером (Redis, RabbitMQ): надёжность, "
      "повторы, расписание, но и лишняя инфраструктура. Начинают с BackgroundTasks, "
      "переезжают на очередь, когда задачи стали важными или долгими.")

    q(9, "Как работает Depends и когда вызываются зависимости?",
      "Перед вызовом ручки FastAPI строит дерево зависимостей и вызывает каждую один раз на "
      "запрос, результат кэшируется внутри запроса. Зависимость с " + m("yield") + " получает "
      "«после»: код после " + m("yield") + " выполняется, когда ответ отправлен. Так устроен "
      + m("get_session") + ": сессия создаётся до ручки и закрывается после. Зависимости "
      "можно вешать на весь роутер, как " + m("require_admin") + " на админские ручки.", ref=38)

    q(10, "Опишите путь запроса от браузера до базы в вашем проекте.",
      "Браузер → nginx (TLS, статика, " + m("/api/") + " проксируется дальше) → uvicorn → "
      "FastAPI: роутер разбирает путь и параметры, Pydantic валидирует тело, зависимости "
      "создают сессию и сервис → сервис строит запрос SQLAlchemy → asyncpg отправляет SQL "
      "в PostgreSQL → результат маппится в объекты модели → роутер превращает их в схему "
      "ответа → JSON уходит обратно. Для страниц сайта перед этим стоит ещё Next.js, который "
      "ходит в тот же API с сервера.", ref=38)

    # ---------- PostgreSQL ----------
    A(P("PostgreSQL", "h1"))

    q(11, "Расшифруйте ACID.",
      "Atomicity — транзакция выполняется целиком или откатывается целиком. Consistency — "
      "после транзакции все ограничения выполнены. Isolation — параллельные транзакции не "
      "видят промежуточных состояний друг друга. Durability — после COMMIT данные не "
      "потеряются при сбое. Пример из проекта: лайк — это вставка в " + m("article_reactions")
      + " и увеличение " + m("likes_count") + " в одной транзакции; половины быть не может.",
      ref=11)

    q(12, "Какие уровни изоляции есть и какой по умолчанию?",
      "Read Uncommitted (в PostgreSQL ведёт себя как следующий), Read Committed — по "
      "умолчанию: каждый запрос видит данные, закоммиченные к его началу. Repeatable Read — "
      "вся транзакция видит снимок на момент начала. Serializable — как будто транзакции "
      "выполнялись по очереди, при конфликте одна откатывается. Чем строже, тем дороже и "
      "больше откатов. Для сайта достаточно Read Committed, для расчётов денег берут "
      "Repeatable Read или Serializable.", ref=11)

    q(13, "Что такое MVCC?",
      "Multi-Version Concurrency Control: PostgreSQL не перезаписывает строку при UPDATE, а "
      "создаёт новую версию, старая остаётся видимой транзакциям, которые начались раньше. "
      "Поэтому читатели не блокируют писателей и наоборот. Плата — мёртвые версии строк, "
      "которые убирает VACUUM. Отсюда же ответ, почему таблица «раздувается» после массовых "
      "UPDATE.", ref=12)

    q(14, "Что такое грязное, неповторяющееся и фантомное чтение?",
      "Грязное — увидеть незакоммиченные данные другой транзакции; в PostgreSQL невозможно. "
      "Неповторяющееся — дважды прочитать одну строку внутри транзакции и получить разные "
      "значения, потому что между чтениями кто-то закоммитил; возможно на Read Committed. "
      "Фантомное — повторный запрос по условию возвращает другое число строк. Repeatable Read "
      "устраняет оба последних.", ref=11)

    q(15, "Что такое дедлок и как его избежать?",
      "Две транзакции ждут друг друга: первая заблокировала строку A и хочет B, вторая — "
      "наоборот. PostgreSQL обнаруживает цикл и откатывает одну из них с ошибкой. "
      "Профилактика: обновлять строки в одном и том же порядке во всех транзакциях, держать "
      "транзакции короткими, не делать сетевых вызовов внутри открытой транзакции. Наш "
      + m("update_counters") + " меняет одну строку одним запросом, циклов там быть не может.",
      ref=11)

    q(16, "Какие типы индексов есть в PostgreSQL?",
      "B-tree — по умолчанию, для равенства, диапазонов, сортировки; наши индексы все такие. "
      "Hash — только равенство, редко нужен. GIN — для «содержит»: JSONB, массивы, "
      "полнотекстовый поиск. GiST — геометрия и диапазоны. BRIN — для огромных таблиц с "
      "естественным порядком, например логов по времени. Ещё бывают уникальные, составные, "
      "частичные (с WHERE) и функциональные (по выражению, например " + m("lower(email)") + ").",
      ref=5)

    q(17, "Когда индекс не используется, хотя он есть?",
      "Когда таблица маленькая и полный перебор дешевле. Когда условие не совпадает с "
      "индексом: " + m("LIKE '%текст'") + " с процентом в начале, функция над колонкой "
      + m("lower(title) = ...") + " без функционального индекса, приведение типа. Когда "
      "составной индекс начинается не с той колонки, по которой фильтруем. Когда селективность "
      "низкая: условие возвращает половину таблицы. Проверяется через " + m("EXPLAIN ANALYZE") + ".",
      ref=5)

    q(18, "Как выбрать порядок колонок в составном индексе?",
      "Сначала колонки с равенством, потом колонка диапазона или сортировки. Индекс "
      + m("(section, published_at)") + " ускорит запрос «раздел равен news, сортировка по "
      "дате», потому что база найдёт блок раздела, а внутри строки уже упорядочены. Обратный "
      "порядок для этого запроса почти бесполезен. Индекс по (a, b) также работает для "
      "запросов только по a, но не только по b.", ref=5)

    q(19, "Как читать EXPLAIN ANALYZE?",
      "Читается снизу вверх и изнутри наружу: сначала выполняются вложенные узлы. Смотрим тип "
      "узла (Seq Scan — перебор, Index Scan — по индексу, Nested Loop / Hash Join — способ "
      "соединения), оценку строк против фактической (большое расхождение — устаревшая "
      "статистика, нужен " + m("ANALYZE") + ") и время. " + m("ANALYZE") + " реально выполняет "
      "запрос, поэтому для UPDATE его оборачивают в транзакцию с ROLLBACK.", ref=5)

    q(20, "Что такое нормализация и когда её нарушают?",
      "Разложение данных по таблицам так, чтобы каждый факт хранился один раз: название тега "
      "в " + m("tags") + ", связь — в " + m("article_tags") + ". Первая нормальная форма — нет "
      "списков в ячейке, вторая — нет зависимостей от части ключа, третья — нет зависимостей "
      "между неключевыми колонками. Нарушают ради скорости чтения: наши "
      + m("likes_count") + " и " + m("views_count") + " — денормализация, их можно посчитать "
      "по таблицам реакций, но считать при каждом показе карточки дорого.", ref=4)

    q(21, "Чем PRIMARY KEY отличается от UNIQUE?",
      "Оба гарантируют уникальность и создают индекс. Первичный ключ — один на таблицу, не "
      "допускает NULL и по смыслу идентифицирует строку; на него ссылаются внешние ключи. "
      "UNIQUE-ограничений может быть много, и NULL там разрешён сколько угодно раз. У "
      + m("articles") + " первичный ключ " + m("id") + ", а " + m("slug") + " — UNIQUE.", ref=4)

    q(22, "Что делает внешний ключ и какие варианты ON DELETE бывают?",
      "Гарантирует, что значение ссылается на существующую строку родителя, и задаёт "
      "поведение при удалении родителя: RESTRICT — запретить, CASCADE — удалить детей, "
      "SET NULL — обнулить ссылку, SET DEFAULT. У нас связи статьи с тегами, просмотрами и "
      "реакциями удаляются каскадом. Индекс на колонку внешнего ключа PostgreSQL не создаёт, "
      "его добавляют вручную.", ref=4)

    q(23, "Что такое VACUUM и зачем он нужен?",
      "Из-за MVCC после UPDATE и DELETE остаются мёртвые версии строк. VACUUM помечает их "
      "место как свободное для повторного использования и обновляет статистику для "
      "планировщика (ANALYZE). Autovacuum делает это сам по порогам. VACUUM FULL "
      "физически сжимает таблицу, но блокирует её, поэтому на проде его почти не запускают.",
      ref=12)

    q(24, "Когда хранить данные в JSONB, а когда в отдельной таблице?",
      "JSONB — для данных с переменной структурой, которые читаются целиком и по которым не "
      "нужно ссылаться внешними ключами: оглавление статьи, настройки, сырой ответ внешнего "
      "API. Таблица — когда по данным нужно искать, соединять, агрегировать, ограничивать "
      "уникальность. Теги в JSONB были бы ошибкой: нельзя сделать «все статьи с тегом X» "
      "эффективно и нельзя гарантировать, что тег существует.", ref=3)

    q(25, "Чем timestamp отличается от timestamptz?",
      m("timestamp") + " хранит настенное время без пояса, " + m("timestamptz") + " — момент "
      "времени, внутри UTC, при показе переводится в пояс сессии. Во втором случае 09:00 "
      "Новосибирска и 05:00 Москвы — одно значение. Правило: всегда timestamptz, в SQLAlchemy "
      + m("DateTime(timezone=True)") + ", даты в приложении — с " + m("tzinfo") + ".", ref=3)

    q(26, "Как делаются бэкапы PostgreSQL?",
      m("pg_dump") + " — логический дамп: SQL-команды для пересоздания схемы и данных, "
      "просто восстановить, годится для наших объёмов. Физический бэкап плюс архив WAL "
      "(журнала транзакций) даёт восстановление на любой момент времени (PITR), но сложнее в "
      "настройке. Репликация — потоковая копия на другой сервер для отказоустойчивости и "
      "чтения; бэкап она не заменяет, потому что ошибочный DELETE реплицируется тоже.", ref=12)

    # ---------- SQL ----------
    A(P("SQL", "h1"))

    q(27, "Какие виды JOIN бывают?",
      "INNER — только совпавшие пары. LEFT — все строки слева, справа NULL при отсутствии пары; "
      "RIGHT — зеркально. FULL — всё с обеих сторон. CROSS — каждая с каждой. Пример: "
      "«все статьи с числом тегов, включая статьи без тегов» — это LEFT JOIN с "
      + m("count(at.tag_id)") + ", потому что INNER потерял бы статьи без тегов.", ref=7)

    q(28, "Чем WHERE отличается от HAVING?",
      "WHERE фильтрует строки до группировки и не видит агрегатов. HAVING фильтрует группы "
      "после GROUP BY и работает с " + m("count") + ", " + m("sum") + ". «Теги, у которых больше "
      "пяти статей» — HAVING; «статьи раздела news» — WHERE. Если условие можно выразить в "
      "WHERE, оно там и должно быть: меньше строк дойдёт до группировки.", ref=8)

    q(29, "В чём разница между count(*), count(колонка) и count(DISTINCT)?",
      m("count(*)") + " считает строки. " + m("count(колонка)") + " — строки, где колонка не "
      "NULL. " + m("count(DISTINCT колонка)") + " — число разных значений. Классическая ловушка: "
      + m("count(at.tag_id)") + " после LEFT JOIN даёт ноль для статей без тегов, а "
      + m("count(*)") + " дал бы единицу.", ref=8)

    q(30, "Подзапрос, JOIN или EXISTS — что выбрать?",
      "Если нужны колонки из второй таблицы — JOIN. Если нужно только проверить наличие "
      "связанных строк — EXISTS: он останавливается на первой найденной и не размножает "
      "строки. " + m("IN (подзапрос)") + " читается проще, но на больших списках медленнее. "
      "Наш фильтр по тегу — " + m("Article.tags.any(Tag.slug == ...)") + ", это EXISTS.", ref=9)

    q(31, "Что такое оконные функции?",
      "Функции, которые считают значение по группе строк, не сворачивая их: каждая строка "
      "остаётся, рядом появляется " + m("row_number()") + ", " + m("sum() OVER (...)") + ", "
      + m("lag()") + ". " + m("PARTITION BY") + " делит на группы, " + m("ORDER BY") + " внутри "
      "окна задаёт порядок. Мы использовали " + m("row_number()") + ", чтобы пронумеровать "
      "запланированные новости и раздать им даты назад по одной в день.", ref=9)

    q(32, "Как сделать «вставить или обновить» одним запросом?",
      m("INSERT ... ON CONFLICT (колонки) DO UPDATE SET ... = EXCLUDED....") + " или "
      + m("DO NOTHING") + ". Конфликт определяется по уникальному ограничению. Это атомарно и "
      "снимает гонку «проверил — вставил». В SQLAlchemy берётся " + m("insert") + " из "
      + m("sqlalchemy.dialects.postgresql") + " с методом " + m("on_conflict_do_update") + ".",
      ref=10)

    # ---------- SQLAlchemy ----------
    A(P("SQLAlchemy", "h1"))

    q(33, "Что такое Core и ORM в SQLAlchemy?",
      "Core — конструктор SQL: " + m("select") + ", " + m("insert") + ", " + m("Table") + ", "
      "результат — строки. ORM надстроен над Core: классы-модели, объекты, сессия с identity "
      "map и unit of work. В 2.0 они используют один язык запросов: " + m("select(Article)")
      + " возвращает объекты, " + m("select(Article.id, Article.title)") + " — строки. У нас "
      "промежуточная таблица " + m("article_tags") + " — чистый Core, остальное — ORM.", ref=13)

    q(34, "Engine, Connection, Session — кто за что отвечает?",
      "Engine — один на приложение: URL, диалект, пул соединений. Connection — одно "
      "соединение с базой, взятое из пула, уровень Core. Session — рабочее пространство ORM: "
      "берёт Connection, ведёт транзакцию, хранит объекты, отправляет изменения. У нас "
      "сессия создаётся на каждый HTTP-запрос через " + m("get_session") + ".", ref=37)

    q(35, "Что такое identity map?",
      "Словарь «первичный ключ → объект» внутри сессии. Одна строка базы — один объект "
      "Python в рамках сессии, повторный " + m("db.get") + " вернёт тот же объект без запроса. "
      "Это защищает от расхождения копий, но объясняет ловушку: после commit объект в карте "
      "остаётся, а его вычисленные базой колонки протухли; чтобы перечитать, нужен "
      + m("refresh") + " или " + m("populate_existing") + ".", ref=38)

    q(36, "Чем flush отличается от commit?",
      m("flush") + " отправляет накопленные INSERT/UPDATE/DELETE в базу внутри открытой "
      "транзакции: после него у новых объектов есть id, а нарушение уникальности уже "
      "поймано. " + m("commit") + " делает flush и фиксирует транзакцию. Мы делаем "
      + m("flush") + " при регистрации просмотра, чтобы поймать " + m("IntegrityError") + " до "
      "увеличения счётчика.", ref=38)

    q(37, "Что такое expire_on_commit и почему в async его выключают?",
      "После commit сессия по умолчанию помечает атрибуты всех объектов устаревшими, и "
      "следующее обращение к ним делает запрос. В асинхронном коде обращение к атрибуту — не "
      + m("await") + ", запрос выполнить негде, получается " + m("MissingGreenlet") + ". "
      "Поэтому " + m("expire_on_commit=False") + ", а колонки, которые вычислила база "
      "(" + m("updated_at") + "), перечитываем явно.", ref=38)

    q(38, "Что такое N+1 и как его увидеть?",
      "Загрузили N статей одним запросом, потом для каждой обратились к " + m("article.tags")
      + " — и ORM сделал ещё N запросов. Видно через " + m("echo=True") + " или логи: "
      "десятки одинаковых SELECT подряд. Лечится стратегией загрузки: " + m("selectinload")
      + " грузит связи одним дополнительным запросом " + m("WHERE id IN (...)") + ", "
      + m("joinedload") + " — одним JOIN. У нас теги объявлены с " + m("lazy='selectin'") + ".",
      ref=27)

    q(39, "selectinload или joinedload?",
      m("joinedload") + " — один запрос с JOIN, хорошо для связей «к одному» и маленьких "
      "коллекций, но размножает строки родителя. " + m("selectinload") + " — два запроса, "
      "второй по списку id, лучше для коллекций: строки не дублируются, результат компактнее. "
      "Для тегов статей выбран selectin. В async ленивая загрузка по умолчанию не работает "
      "вообще, поэтому стратегию задают явно или через " + m("lazy='raise'") + ", чтобы "
      "ошибка была понятной.", ref=27)

    q(40, "Чем cascade у relationship отличается от ondelete у ForeignKey?",
      m("ondelete='CASCADE'") + " — правило базы: сработает при любом удалении, даже из "
      "psql, и выполнится одним запросом. " + m("cascade='all, delete-orphan'") + " — правило "
      "ORM: сессия сама удалит связанные объекты, но только если удаление идёт через неё и "
      "объекты загружены. Надёжнее полагаться на базу, а cascade в ORM использовать для "
      "удобства работы с объектами.", ref=28)

    q(41, "Почему сессия создаётся на каждый запрос, а не одна на приложение?",
      "Сессия — это транзакция и набор состояний объектов. Одна общая сессия смешала бы "
      "данные разных пользователей, не потокобезопасна, накапливала бы объекты в памяти и "
      "держала транзакцию открытой вечно. Сессия на запрос даёт изоляцию, короткие "
      "транзакции и автоматическую уборку через зависимость с " + m("yield") + ".", ref=38)

    q(42, "Как подобрать размер пула соединений?",
      "Соединений нужно столько, сколько запросов реально ждут базу одновременно; обычно "
      "5–20 на воркер. Больше — не быстрее: PostgreSQL сам ограничен числом ядер и лимитом "
      + m("max_connections") + ", а лишние соединения едят память сервера. Считать надо "
      "суммарно по всем воркерам и сервисам. При нехватке будет ошибка "
      + m("QueuePool limit reached") + ", при избытке — отказ базы в подключении.", ref=37)

    q(43, "Когда лучше написать сырой SQL вместо ORM?",
      "Для сложной аналитики с оконными функциями и CTE, для массовых обновлений и для "
      "диалектных возможностей, которых нет в ORM. Пишется через " + m("text()") + " или "
      "Core-выражения, обязательно с параметрами, а не f-строками — иначе SQL-инъекция. Наш "
      "запрос с датами публикации был удобнее в чистом SQL, а обычные CRUD-операции — в ORM.",
      ref=31)

    q(44, "Зачем миграции, если есть create_all? Что не умеет autogenerate?",
      m("create_all") + " только создаёт отсутствующие таблицы и никогда не меняет "
      "существующие. Миграции — версионированные шаги изменения схемы с откатом, применяются "
      "одинаково на всех окружениях. Autogenerate не видит переименований (предложит удалить "
      "и создать — данные потеряются), не всегда видит смену типа и default, не пишет "
      "миграции данных. Поэтому черновик читают и правят руками.", ref=43)

    q(45, "Что такое Mapped и mapped_column и как определяется nullable?",
      m("Mapped[T]") + " — аннотация типа, из неё SQLAlchemy берёт тип Python и "
      "обязательность: " + m("Mapped[str]") + " даёт NOT NULL, " + m("Mapped[str | None]")
      + " разрешает NULL. " + m("mapped_column(...)") + " добавляет то, чего нет в типе: длину "
      "строки, индекс, default, внешний ключ. Аннотация нужна и для IDE, и для генерации "
      "схемы, поэтому дублировать " + m("nullable=") + " не нужно.", ref=14)

    # ---------- архитектура ----------
    A(P("Архитектура", "h1"))

    q(46, "Зачем слой сервисов между роутером и моделями?",
      "Роутер отвечает за HTTP: параметры, коды ответа, схемы. Сервис — за данные и правила: "
      "запросы, транзакции, «черновик считается ненайденным». Разделение позволяет вызывать "
      "логику из скриптов и тестов без HTTP, держать роутеры короткими и менять хранение, не "
      "трогая API. Зависимости идут в одну сторону: роутеры → сервисы → модели.", ref=38)

    q(47, "Где ловить ошибки и как превращать их в HTTP-ответы?",
      "Сервис бросает доменные исключения: " + m("ArticleNotFoundError") + ", "
      + m("TagNotFoundError") + ", " + m("UploadError") + ". Он не знает про HTTP. Роутер ловит "
      "их и переводит в " + m("HTTPException") + " с нужным кодом: 404, 422. Ошибки валидации "
      "входа Pydantic отдаёт 422 сам. Непредвиденные исключения не ловим — пусть будет 500 с "
      "записью в лог, это честнее тихой поломки.", ref=39)

    q(48, "Зачем ограничения в базе, если есть валидация Pydantic?",
      "Это два слоя. Pydantic защищает вход API: типы, диапазоны, обязательные поля. База "
      "защищает данные от всех источников сразу: миграций, скриптов, ручных правок, будущего "
      "кода без валидации. И только база может гарантировать уникальность и ссылочную "
      "целостность при параллельных запросах. Пример: " + m("Literal[1, -1]") + " в схеме и "
      + m("CHECK (value IN (-1, 1))") + " в таблице реакций.", ref=4)

    q(49, "Как бороться с гонками при параллельных запросах?",
      "Не проверять в коде «есть ли уже», а полагаться на уникальный ключ и ловить "
      + m("IntegrityError") + " или использовать " + m("ON CONFLICT") + ". Счётчики менять "
      "выражением в базе (" + m("views_count = views_count + 1") + "), а не читать-прибавить-"
      "записать. Держать транзакции короткими. Делать операции идемпотентными: повторный "
      "лайк тем же значением ничего не меняет, повторный запуск скрипта загрузки не создаёт "
      "дублей.", ref=11)

    q(50, "Как вы проектируете схему под новую функцию?",
      "Сначала сущности и связи словами: статья, тег, статья имеет много тегов — значит "
      "M:N и промежуточная таблица. Потом для каждой колонки: тип, обязательность, "
      "уникальность, ограничения, что индексировать под известные запросы. Затем правила "
      "удаления. Потом модель SQLAlchemy, миграция, сервис, схемы API, тест. Пример: раздел "
      "«Новости» потребовал одну колонку " + m("section") + " с default, индекс и фильтр в "
      "запросах — вместо второй таблицы статей.", ref=46)

    A(Spacer(1, 8))
    A(note("<b>Как готовиться.</b> Не зубрите ответы. По каждому вопросу найдите место в "
           "своём коде, где это применено, и объясните вслух, почему сделано именно так. "
           "Интервьюер почти всегда уходит с вопроса на проект: «а у вас как?» — и это лучший "
           "момент собеседования.", GOOD_BG))

    return S
